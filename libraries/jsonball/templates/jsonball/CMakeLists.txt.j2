
add_library( knowledgr_jsonball
   mm_ticks.cpp
   rc_params.cpp
   ${CMAKE_CURRENT_SOURCE_DIR}/include/knowledgr/jsonball/jsonball.hpp
)

target_link_libraries( knowledgr_jsonball fc )

target_include_directories( knowledgr_jsonball PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include" )

INSTALL( TARGETS
   knowledgr_jsonball

   RUNTIME DESTINATION bin
   LIBRARY DESTINATION lib
   ARCHIVE DESTINATION lib
)
